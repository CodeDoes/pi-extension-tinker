{
	"name": "pi-extension-tinker",
	"version": "0.1.0",
	"type": "module",
	"description": "Tinker (Thinking Machines) provider extension for pi-coding-agent. Uses Tinker's OpenAI-compatible chat completions endpoint.",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/CodeDoes/pi-extension-tinker.git"
	},
	"scripts": {
		"clean": "echo nothing to clean",
		"build": "echo nothing to build",
		"check": "echo nothing to check",
		"test": "tsx test.ts"
	},
	"pi": {
		"extensions": ["./index.ts"]
	},
	"dependencies": {
		"@earendil-works/pi-ai": "^0.80.7",
		"@earendil-works/pi-coding-agent": "^0.80.7"
	},
	"devDependencies": {
		"tsx": "^4.19.0"
	}
}

